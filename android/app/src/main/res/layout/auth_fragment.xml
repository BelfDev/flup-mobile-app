<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/authContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@mipmap/background_image"
        tools:context=".authentication.ui.AuthFragment"
        android:focusableInTouchMode="true"
        android:clickable="true"
        android:focusable="true">

    <ImageView
            android:id="@+id/logoImageView"
            android:src="@mipmap/logo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_marginTop="40dp"
            android:contentDescription="@string/app_name"/>

    <include
            android:id="@+id/authRearFormView"
            layout="@layout/auth_rear_form_view"
            android:layout_width="0dp"
            android:layout_height="243dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="@+id/authFormView"
            android:layout_marginStart="40dp"
            android:layout_marginEnd="40dp"
            android:layout_marginBottom="16dp">
    </include>

    <include
            android:id="@+id/authFormView"
            layout="@layout/auth_form_view"
            android:layout_width="0dp"
            android:layout_height="243dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginEnd="32dp"
            android:layout_marginStart="32dp"/>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/authFormFAB"
            android:src="@drawable/ic_arrow_forward"
            android:tint="@color/black"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:elevation="8dp"
            app:fabSize="normal"
            app:layout_constraintEnd_toEndOf="@+id/authFormView"
            android:layout_marginEnd="24dp"
            app:layout_constraintTop_toBottomOf="@+id/authFormView"
            app:layout_constraintBottom_toBottomOf="@+id/authFormView"/>

</androidx.constraintlayout.widget.ConstraintLayout>